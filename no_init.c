#include <stdio.h>

int main (int argc, char *argv[]) {
	char character;
	int number;

	printf("character is = %c\n", character);
	printf("number is = %d\n", number);
	// Archで実行
	//character is =
	// number is = 0
	return 0;
}
